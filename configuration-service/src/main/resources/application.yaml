spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/leoluzh/shopping-microservices-with-spring-cloud-config
          clone-on-start: true
          default-label: main

server:
  port: 8888
  shutdown: graceful
